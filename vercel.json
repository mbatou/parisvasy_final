{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "export DATABASE_URL=\"${DATABASE_URL:-$POSTGRES_PRISMA_URL}\" && export DIRECT_URL=\"${DIRECT_URL:-$POSTGRES_URL_NON_POOLING}\" && prisma generate && if [ -n \"$DATABASE_URL\" ]; then prisma db push --skip-generate --accept-data-loss; fi && next build",
  "regions": ["cdg1"],
  "crons": [],
  "headers": [
    {
      "source": "/api/stripe/webhook",
      "headers": [
        {
          "key": "Content-Type",
          "value": "application/json"
        }
      ]
    }
  ]
}
